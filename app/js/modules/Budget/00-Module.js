
_module = angular.module('module.Budget');
console.log(_module.name, '=', _module );

//-------------------------------------------------------------------------------------------------------------------//
// CREATES DIRECTIVES
// -> must be done before app bootstrapping (and config)
//-------------------------------------------------------------------------------------------------------------------//
